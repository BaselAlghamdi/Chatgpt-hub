import {ArrowUpRight,Mail} from 'lucide-react';
import {getProfile,listEntries} from '@/lib/server/data';
import {emailComposeUrl} from '@/lib/content';
import {Credentials} from '@/components/portfolio';
export const dynamic='force-dynamic';
export const metadata={title:'About'};
export default async function About(){const[p,certificates,honors]=await Promise.all([getProfile(),listEntries('certificate'),listEntries('honor')]);const email=emailComposeUrl(p.email);return <main id="main-content" className="wrap about-page"><section className="about-top"><div><p className="eyebrow">A LITTLE ABOUT ME</p><h1>{p.name}</h1><p className="lead">{p.intro}</p></div><div className="about-profile-card"><p className="eyebrow">{p.university}</p><h2>{p.degree}</h2><p>{p.faculty}</p><p>{p.location}</p></div></section><div className="about-content"><section><h2>Learning through the work.</h2>{p.bio.split('\n\n').map((s,i)=><p key={i}>{s}</p>)}<div className="education-detail"><div><h3>Education</h3><p>{p.university}</p><p>{p.degree} · {p.faculty}</p></div></div><div className="honors-section"><h3>Academic honors & distinction</h3>{honors.length?<Credentials entries={honors}/>:<p>Academic recognition will be added here.</p>}</div></section><section><h2>Where my attention goes.</h2><ul className="interest-list">{p.interests.map((s,i)=><li key={i}><span>{String(i+1).padStart(2,'0')}</span><h3>{s}</h3></li>)}</ul><p>If you have feedback on my research or would like to connect, I’d be glad to hear from you.</p><div className="contact-actions"><a className="button" href={email} target="_blank" rel="noopener noreferrer"><Mail size={17}/>Email</a><a className="button secondary" href={p.linkedin} target="_blank" rel="noopener noreferrer">LinkedIn <ArrowUpRight size={16}/></a></div></section></div><section className="certifications-section"><div className="section-heading"><h2>Professional certifications</h2></div>{certificates.length?<Credentials entries={certificates}/>:<p className="muted">Certifications will be added here.</p>}</section></main>}
