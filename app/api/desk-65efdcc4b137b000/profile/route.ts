import {authorize} from '@/lib/server/auth';import {profileInput} from '@/lib/content';import {saveProfile} from '@/lib/server/data';import {apiError,json,requestJson} from '@/lib/server/http';export async function PUT(r:Request){try{const denied=await authorize(r,true);if(denied)return denied;return json(await saveProfile(profileInput.parse(await requestJson(r))))}catch(e){return apiError(e)}}