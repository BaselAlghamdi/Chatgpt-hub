import {pageMetadata} from '@/lib/server/seo';
import {publicEntries} from '@/lib/server/public-content';import {ProjectRow} from '@/components/portfolio';export const dynamic='force-dynamic';export const metadata=pageMetadata('/projects','Projects','Explore financial models, reports, presentations, and other projects by Basel Alghamdi.');export default async function Projects(){const result=await publicEntries('project');const entries=result.entries;return <main id="main-content" className="wrap projects-page"><header className="page-heading"><p className="eyebrow">THE WORK BEHIND THE NUMBERS</p><h1>Selected projects.</h1><p>Financial models, reports, and presentations.</p></header>{result.unavailable?<p role="status">Projects are temporarily unavailable. Please try again later.</p>:entries.length?<div className="project-list">{entries.map((p,i)=><ProjectRow key={p.id} project={p} index={i}/>)}</div>:<div className="empty-result"><h2>No projects published yet.</h2></div>}</main>}
