import type {MetadataRoute} from 'next';import {listEntries} from '@/lib/server/data';import {entryLink} from '@/lib/content';export const dynamic='force-dynamic';export default async function sitemap():Promise<MetadataRoute.Sitemap>{if(!process.env.SITE_URL)return[];const origin=process.env.SITE_URL;const entries=await listEntries();return[...['/','/about','/research','/projects'].map(path=>({url:new URL(path,origin).href})),...entries.filter(e=>e.kind==='article'||e.kind==='project').map(e=>({url:new URL(entryLink(e),origin).href,lastModified:e.updatedAt}))]}