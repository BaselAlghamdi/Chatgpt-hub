import {notFound} from 'next/navigation';import {adminSession} from '@/lib/server/auth';import {getEntry} from '@/lib/server/data';import {kinds,newEntry,type EntryKind} from '@/lib/content';import {EntryEditor} from '@/components/admin';import {AdminGate} from '@/components/admin-gate';export const dynamic='force-dynamic';export const metadata={title:'Edit content',robots:{index:false,follow:false}};export default async function Editor({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{kind?:string}>}){if(!await adminSession())return <AdminGate/>;const{id}=await params;const kind=(await searchParams).kind;const entry=id==='new'?newEntry(kinds.includes(kind as EntryKind)?kind as EntryKind:'article'):await getEntry(id);if(!entry)notFound();return <main id="main-content" className="wrap admin-page"><EntryEditor initial={entry}/></main>}