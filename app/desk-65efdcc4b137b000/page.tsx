import {adminSession} from '@/lib/server/auth';import {getProfile,listEntries} from '@/lib/server/data';import {AdminDashboard} from '@/components/admin';import {AdminGate} from '@/components/admin-gate';export const dynamic='force-dynamic';export const metadata={title:'Publishing desk',robots:{index:false,follow:false}};export default async function Admin(){if(!await adminSession())return <AdminGate/>;const[entries,profile]=await Promise.all([listEntries(undefined,false),getProfile(true)]);return <main id="main-content" className="wrap admin-page"><AdminDashboard entries={entries} profile={profile}/></main>}
