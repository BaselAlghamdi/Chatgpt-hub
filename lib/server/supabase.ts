import 'server-only';
export function configured(){return !!(process.env.SUPABASE_URL&&process.env.SUPABASE_SERVICE_ROLE_KEY)}
export function config(){const url=process.env.SUPABASE_URL?.replace(/\/$/,'');const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('Content service is not configured.');if(new URL(url).protocol!=='https:')throw new Error('HTTPS is required for the content service.');return{url,key}}
export async function service(path:string,init:RequestInit={}){const{url,key}=config();const r=await fetch(url+path,{...init,cache:'no-store',headers:{apikey:key,Authorization:'Bearer '+key,'Content-Type':'application/json',...init.headers},signal:AbortSignal.timeout(20000)});if(!r.ok){const e=await r.json().catch(()=>({}));const err=new Error(e.message||e.error_description||'Content service unavailable') as Error&{code?:string;status?:number};err.code=e.code;err.status=r.status;throw err}return r}
export async function rest(path:string,init:RequestInit={}){const r=await service('/rest/v1/'+path,init);if(r.status===204)return null;const text=await r.text();return text?JSON.parse(text):null}
