'use client';import {useState} from 'react';import {Field} from './admin';export function AdminLogin(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);return <form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/desk-65efdcc4b137b000/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Sign-in failed.');window.location.assign('/desk-65efdcc4b137b000')}catch(e){setError((e as Error).message);setBusy(false)}}}><p>Sign in to manage your research, projects, and credentials.</p><Field label="Email"><input type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/></Field><Field label="Password"><input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></Field><button className="button" disabled={busy}>{busy?'Signing in…':'Sign in'}</button>{error&&<p role="alert" className="form-error">{error}</p>}</form>}
export function AdminLogout(){return <button className="button secondary" onClick={async()=>{const r=await fetch('/api/desk-65efdcc4b137b000/session',{method:'DELETE'});if(r.ok)window.location.assign('/desk-65efdcc4b137b000')}}>Sign out</button>}
