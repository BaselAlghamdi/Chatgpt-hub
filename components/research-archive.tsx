'use client';
import {useState} from 'react';
import {Search} from 'lucide-react';
import type {Entry} from '@/lib/content';
import {ArticleCard} from '@/components/portfolio';
export function ResearchArchive({articles}:{articles:Entry[]}){const[category,setCategory]=useState('All research');const[query,setQuery]=useState('');const categories=['All research',...new Set(articles.map(a=>a.category).filter(Boolean))];const visible=articles.filter(a=>(category==='All research'||a.category===category)&&[a.title,a.excerpt,a.category,...a.tags].join(' ').toLowerCase().includes(query.trim().toLowerCase()));return <><div className="archive-controls"><div className="filter-buttons" role="group" aria-label="Filter research by topic">{categories.map(c=><button key={c} type="button" aria-pressed={category===c} onClick={()=>setCategory(c)}>{c}</button>)}</div><label className="search-input"><Search size={18}/><span className="sr-only">Search research</span><input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search the journal…"/></label></div><p className="sr-only" aria-live="polite">{visible.length} articles found</p>{visible.length?<div className="article-grid archive-grid">{visible.map(a=><ArticleCard key={a.id} article={a}/>)}</div>:<div className="empty-result"><h2>{articles.length?'No stories found.':'No research published yet.'}</h2>{articles.length>0&&<><p>Try another topic or search term.</p><button className="button secondary" onClick={()=>{setQuery('');setCategory('All research')}}>Show all research</button></>}</div>}</>}
