import {authConfigured} from '@/lib/server/auth';import {AdminLogin} from '@/components/admin-login';export function AdminGate(){return <main id="main-content" className="wrap admin-page"><div className="admin-login"><p className="eyebrow">PRIVATE PUBLISHING DESK</p><h1>Administrator access.</h1>{authConfigured()?<AdminLogin/>:<p>Finish the administrator setup in the deployment guide to sign in. Public pages remain available.</p>}</div></main>}