'use client';
import {useState,useEffect} from 'react';
import {Link as LinkIcon,Printer,Download,Check} from 'lucide-react';
export function ArticleTools({file,label}:{file:string;label:string}){const[copied,setCopied]=useState(false);const[message,setMessage]=useState('');useEffect(()=>{if(!copied)return;const id=setTimeout(()=>{setCopied(false);setMessage('')},3000);return()=>clearTimeout(id)},[copied]);async function copyLink(){try{await navigator.clipboard.writeText(window.location.href);setCopied(true);setMessage('Article link copied.')}catch{setMessage('Copy this link: '+window.location.href)}}return <><div className="article-tools"><button onClick={copyLink}>{copied?<Check size={16}/>:<LinkIcon size={16}/>} {copied?'Link copied':'Copy link'}</button><button className="print-tool" onClick={()=>window.print()}><Printer size={16}/>Print article</button><a href={file} download><Download size={16}/>{label}</a></div><div className="share-message" role="status" aria-live="polite">{message}</div></>}
export function ReadingProgress(){const[progress,setProgress]=useState(0);useEffect(()=>{function update(){const a=document.getElementById('article-content');if(!a)return;const start=a.getBoundingClientRect().top+window.scrollY;const end=start+a.offsetHeight-window.innerHeight;setProgress(Math.max(0,Math.min(100,(window.scrollY-start)/Math.max(1,end-start)*100)))}let frame=0;function schedule(){if(!frame)frame=requestAnimationFrame(()=>{frame=0;update()})}update();window.addEventListener('scroll',schedule,{passive:true});window.addEventListener('resize',schedule);return()=>{cancelAnimationFrame(frame);window.removeEventListener('scroll',schedule);window.removeEventListener('resize',schedule)}},[]);return <div className="reading-progress" style={{width:progress+'%'}} aria-hidden="true"/>}
